import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  wrapper: {
    height: 42,
    justifyContent: 'space-evenly',
    alignItems: 'center',
    borderRadius: 7,
  },
  titleText: {
    color: 'white',
  },
});
